#if !defined(WORD_COUNT_H)
#define WORD_COUNT_H
#include<string>
#include<map>

using namespace std;

namespace word_count {
std::map<string, int> words(string);
}  // namespace word_count

#endif // WORD_COUNT_H